<h1><%= @user.name %></h1>
<p><%= image_tag "#{avatar_for(@user.gender)}.png", size: "100x100" %></p>
<p>
	<%= t(:level) %> : <%= @user.level %> - 
	<%= link_to t(:change_password), change_password_path %>
</p>
<p><%= link_to t(:log_out), logout_path %></p>

<%= render partial: 'user_exams' %>