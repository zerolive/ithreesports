<center>
	<h1><%= t(:buy_courses) %></h1>

	<p>
		<h3><%= t(:buy_text) %></h3>
	</p>

	<% @courses.each do |course| %>
		<p><%= buy_link(course.name, course.price) %></p>
	<% end %>
	<p>
		<%= link_to t(:back), signin_path %>
	</p>
</center>
